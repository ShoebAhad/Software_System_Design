// Scoring Class
public class Scoring {
    public int calculateSinglePlayerScore() {
        // Simulated logic for calculating single-player score
        int score = (int) (Math.random() * 1000);
        return score;
    }

    public int calculateTeamScore() {
        // Simulated logic for calculating team-based score
        int score = (int) (Math.random() * 2000);
        return score;
    }
}
